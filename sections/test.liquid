{{ section.settings.test }}
{% schema %}
{
   "name": "test",
   "settings": [
   {
      "type": "liquid",
      "id": "test",
      "label": "test",
      "info": "Add app snippets or other liquid code to create advanced customizations."
   }],
   "presets": [
   {
      "name": "Add"
   }]
}
{% endschema %}